import './App.css';
import Button from "./compnent/button";


function App() {
    return (
        <div className="App">
            <header className="App-header">

                <Button />
                {/* 리액트에서 컴포넌트 name의 경우 파스칼케이스 (대문자시작) 으로 진행해야한다. */}
            </header>
        </div>
    );
}

export default App;
